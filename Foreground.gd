extends TileMap


"""var current_tile_scenes = []
func _ready():
	current_tile_scenes.resize(12)
var torch_scene = preload("res://Tiles/Torch.tscn")
	
func place_tile_scenes():
	var torch_tiles = get_used_cells_by_id(0)
	for tile in torch_tiles:
		if tile not in current_tile_scenes[0]:
			
	
	current_tile_scenes[0] = torch_tiles
	
func tiles_instancing(tile_array, tile_scene, room):
	for tile in tile_array:
		var tile_instance = tile_scene.instance()
		tile_instance.position = map_to_world(tile)
		tile_instance.tile = tile
		room.add_child(tile_instance)"""
